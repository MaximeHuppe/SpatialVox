numpy>=1.26
nibabel>=5.2
pyyaml>=6.0
pytest>=8.0

# Install the torch build that matches the hardware: the default wheel ships MPS
# support on Apple Silicon; on an NVIDIA card install the matching CUDA build.
torch>=2.4

# Optional: set logging.backend to wandb in configs/config.yaml to mirror the
# metrics that already go to runs/<name>/metrics.jsonl.
wandb>=0.19

# Optional: the interactive 3D view in notebooks/demo.py.
plotly>=5.20
